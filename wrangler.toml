# Cloudflare Workers Static Assets 部署配置
# CI 使用 `wrangler deploy`，通过 [assets] 上传 ./dist 作为纯静态站点

name = "px-reader"
compatibility_date = "2024-01-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"

# [vars]
# VITE_API_BASE_URL = "https://px-reader-api.workers.dev"
