# Cloudflare Pages 部署配置
# 如果使用 wrangler CLI 部署，此文件会被读取

name = "px-reader"
compatibility_date = "2024-01-01"

# 纯静态 SPA，不需要 Workers 代码
# 使用 `npx wrangler pages deploy dist --project-name=px-reader` 部署

# [vars]
# VITE_API_BASE_URL = "https://px-reader-api.workers.dev"
